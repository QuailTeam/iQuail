UID for solutions?

install uninstall to specific class

limitation ram mkdtemp

ftp stuck on empty file


Dl - update:
dl >> si modif update (up force)
dl >> si modif pas update (up ignore)

dl >> si supprimé recreer (tous)
